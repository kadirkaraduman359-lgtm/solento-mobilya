[build]
builder = "nixpacks"
buildCommand = "pip install -r depo_web/requirements.txt"

[deploy]
startCommand = "cd depo_web && gunicorn 'app:create_app()' --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/login"
restartPolicyType = "on_failure"
